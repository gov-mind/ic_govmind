# Linode Server Configuration
NODE_ENV=production
PORT=3001

# API Keys (REQUIRED - Replace with your actual keys)
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# CORS - Allow your frontend domains (replace with your actual domains)
CORS_ORIGIN=http://localhost:3000,http://localhost:5173,https://your-ic-frontend.icp0.io

# Rate limiting (requests per 15 minutes per IP)
RATE_LIMIT_MAX=100
